<1. For a matrix, calculate the row sum and column sum
<2. Find the sum of principle diagonal elements and secondary diagonal elements
<3. Depending on user choice, calculate a particular row sum and column sum
4. Exchange a row and the column of a matrix Depending on the user's choice
5. Display the elements of a matrix diagonally
<6. Determine whether a matrix is sparse matrix or not
<7. Sort each row of a matrix
<8. Sort each column of a matrix 
<9. Depending on user choice, exchange the element of a matrix 
<10. Display the upper triangle and lower triangle of a matrix
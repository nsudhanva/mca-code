#include<stdio.h>

struct Node{
    int info;
    struct Node *next;
};

struct Node *is_empty(struct Node *list){
    if(list == NULL){
        return 1;
    }
    return 0;
}

struct Node *insert_at_beg(struct Node *list){
    
}

int main()
{
    
}